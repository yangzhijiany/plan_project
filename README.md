# LLM Task Planner

一个基于 LLM 的智能任务计划生成器，帮助用户自动规划日常学习和工作计划。

## 功能特性

- 🎯 **自然语言任务描述**：用自然语言描述任务，LLM 自动分析并生成子任务
- 🤖 **智能子任务生成**：LLM 根据任务描述自动生成子任务和预计时间
- ⏰ **时间管理**：用户可以修改子任务的预计时间
- 🎨 **任务重要性**：支持设置任务重要性（低、中、高），影响计划分配
- 🔄 **长期任务支持**：支持创建长期任务（无截止日期），自动生成未来30天计划
- 📅 **日历视图**：使用 FullCalendar 可视化显示所有计划，按重要性显示不同颜色
- ✏️ **任务编辑**：可以在日历中修改任务的分配时间
- 🗑️ **任务删除**：可以在日历中删除特定任务项
- 💾 **数据持久化**：使用 SQLite 数据库保存计划数据

## 技术栈

### 后端
- FastAPI - 现代 Python Web 框架
- SQLAlchemy - ORM 数据库操作
- OpenAI API - LLM 集成
- SQLite - 轻量级数据库

### 前端
- React - UI 框架
- Vite - 构建工具
- Tailwind CSS - 样式框架
- FullCalendar - 日历组件
- React Router - 路由管理

## 项目结构

```
task-planner/
├── backend/
│   ├── main.py           # FastAPI 主应用
│   ├── database.py       # 数据库连接配置
│   ├── models.py         # 数据模型
│   ├── requirements.txt  # Python 依赖
│   └── .env              # 环境变量（需要创建）
├── frontend/
│   ├── src/
│   │   ├── App.jsx       # 主应用组件
│   │   ├── InputPage.jsx # 输入页面
│   │   ├── CalendarPage.jsx # 日历页面
│   │   ├── main.jsx      # 入口文件
│   │   └── index.css     # 全局样式
│   ├── package.json      # Node.js 依赖
│   ├── vite.config.js    # Vite 配置
│   └── tailwind.config.js # Tailwind 配置
└── README.md
```

## 安装和运行

### 1. 后端设置

```bash
# 进入后端目录
cd backend

# 创建虚拟环境（推荐）
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# 安装依赖
pip install -r requirements.txt

# 创建 .env 文件
cp .env.example .env
# 编辑 .env 文件，填入你的 OPENAI_API_KEY

# 运行后端服务器
uvicorn main:app --reload
```

后端将在 `http://localhost:8000` 运行。

### 2. 前端设置

```bash
# 进入前端目录
cd frontend

# 安装依赖
npm install

# 运行开发服务器
npm run dev
```

前端将在 `http://localhost:5173` 运行。

## 使用说明

### 1. 创建任务

1. 访问 `http://localhost:5173`
2. 输入任务名称（例如："CS421 Midterm3"）
3. 用自然语言描述任务（例如："要完成复习 CS421 midterm3 的复习，我需要复习 PPT，复习 WA，复习 MP，做 practice quiz"）
4. 选择任务重要性（低、中、高）
5. 选择是否为长期任务（例如：每天做 LeetCode）
6. 如果不是长期任务，设置截止日期
7. 点击"创建任务"按钮

### 2. 生成子任务

1. 任务创建成功后，点击"生成子任务"按钮
2. LLM 会分析任务描述并自动生成子任务列表
3. 查看生成的子任务和预计时间
4. 可以修改每个子任务的预计时间（小时）

### 3. 生成每日计划

1. 确认子任务和时间后，点击"生成每日计划"按钮
2. LLM 会根据子任务和时间生成每日计划
3. 计划会自动保存到数据库

### 4. 查看和管理日历

1. 点击导航栏的"日历视图"
2. 查看所有已保存的计划
3. 根据任务重要性显示不同颜色：
   - 红色：高优先级
   - 橙色：中优先级
   - 绿色：低优先级
4. 点击任务可以查看详情、修改时间或删除任务

## API 端点

### POST /tasks
创建新任务

**请求体**：
```json
{
  "task_name": "CS421 Midterm3",
  "description": "要完成复习 CS421 midterm3 的复习，我需要复习 PPT，复习 WA，复习 MP，做 practice quiz",
  "importance": "high",
  "is_long_term": false,
  "deadline": "2025-12-15"
}
```

### POST /tasks/{task_id}/generate-subtasks
生成子任务

**响应**：
```json
{
  "subtasks": [
    {"id": 1, "subtask_name": "复习 PPT", "estimated_hours": 3.0, "is_completed": false},
    {"id": 2, "subtask_name": "复习 WA", "estimated_hours": 2.5, "is_completed": false},
    {"id": 3, "subtask_name": "复习 MP", "estimated_hours": 2.0, "is_completed": false},
    {"id": 4, "subtask_name": "做 practice quiz", "estimated_hours": 1.5, "is_completed": false}
  ]
}
```

### POST /tasks/{task_id}/generate-plan
生成每日计划

### GET /calendar
获取日历数据

**响应**：
```json
[
  {
    "id": 1,
    "date": "2025-12-01",
    "task_id": 1,
    "task_name": "CS421 Midterm3",
    "subtask_id": 1,
    "subtask_name": "复习 PPT",
    "allocated_hours": 2.0,
    "is_completed": false,
    "importance": "high"
  }
]
```

### PUT /daily-items/{item_id}
更新每日任务项的分配时间

### DELETE /daily-items/{item_id}
删除日历中的特定任务项

### DELETE /tasks/{task_id}
删除任务（会级联删除所有子任务和计划项）

## 环境变量

在 `backend/.env` 文件中设置：

```
OPENAI_API_KEY=your_openai_api_key_here
```

## 数据库

数据库文件 `plans.db` 会自动创建在 `backend` 目录下。

数据库表结构：
- `tasks`: 存储任务信息
  - `id`: 主键
  - `task_name`: 任务名称
  - `description`: 任务描述
  - `importance`: 任务重要性（low, medium, high）
  - `is_long_term`: 是否长期任务
  - `deadline`: 截止日期（可为空）
  - `created_at`: 创建时间

- `subtasks`: 存储子任务信息
  - `id`: 主键
  - `task_id`: 任务 ID（外键）
  - `subtask_name`: 子任务名称
  - `estimated_hours`: 预计时间（小时）
  - `is_completed`: 是否完成
  - `created_at`: 创建时间

- `daily_task_items`: 存储每日任务分配
  - `id`: 主键
  - `date`: 日期
  - `task_id`: 任务 ID（外键）
  - `subtask_id`: 子任务 ID（外键）
  - `allocated_hours`: 分配的时间（小时）
  - `is_completed`: 是否完成
  - `created_at`: 创建时间

- `daily_plans`: 旧表（保留以兼容现有数据）

## 注意事项

1. 确保已安装 Python 3.8+ 和 Node.js 16+
2. 需要有效的 OpenAI API Key
3. 后端和前端需要同时运行
4. 如果同一天已有计划，新计划会自动合并到该日期

## 开发

### 后端开发
```bash
cd backend
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

### 前端开发
```bash
cd frontend
npm run dev
```

## 许可证

MIT License

## 贡献

欢迎提交 Issue 和 Pull Request！

